# CrowdList — Build Spec

The current, authoritative project specification is [BUILD_SPEC.md](./BUILD_SPEC.md).

This pointer replaces the earlier draft, which referenced the superseded AudD and JamBase-setlist approach.
