EXPO_PUBLIC_CROWDLIST_API_URL=http://YOUR_COMPUTER_LAN_IP:5173

# Never put AudD, OpenAI, or backend admin secrets in EXPO_PUBLIC_* variables.
# Those stay server-side in the root .env / hosted environment.
